model = "gpt-5.4"
model_reasoning_effort = "high"
hide_agent_reasoning = true
network_access = true
web_search = "live"
personality = "pragmatic"

[features]
rmcp_client = true
skills = true
unified_exec = true
apps = false
shell_snapshot = true
steer = true

[tui]
status_line = ["current-dir", "git-branch"]

# Hooks are configured in .codex/hooks.json (mirrors .claude/hooks/*).
